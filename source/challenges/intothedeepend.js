const { ChallengeTemplate } = require("../classes");

module.exports = new ChallengeTemplate("Into the Deep End",
	"Start the delve fighting an Artifact Guardian",
	1,
	true,
	false
).setScoreMultiplier(1);
